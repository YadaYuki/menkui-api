{
  "name":"menkui-api",
  "version":"0.1.0",
  "description":"serverless api for menkui camera",
  "scripts": {
    "start":"serverless offline start",
    "deploy":"serverless deploy"
  },
  "devDependencies": {
    "serverless-offline": "^6.8.0"
  },
  "dependencies": {
    "serverless-dotenv-plugin": "^3.1.0",
    "serverless-python-requirements": "^5.1.0"
  }
}
